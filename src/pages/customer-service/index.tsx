import React, { ReactElement } from 'react'
import CustomerService from '../../common/components/customer-service/CustomerService'

const index = (): ReactElement => {
  return (
    <CustomerService />
  )
}

export default index
